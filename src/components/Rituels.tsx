// Legacy component — kept for reference but not rendered in v10
// Services section now handles all service cards with lightbox galleries
import type { Locale } from "@/lib/copy";

type RituelsProps = {
  locale: Locale;
};

export function Rituels(_props: RituelsProps) {
  return null;
}
